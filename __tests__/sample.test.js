describe('sample test 101', () => {
  test('works as t', () => {
    expect(1).toEqual(1)
  })
})

describe('sample test 102', () => {
  test('works as expected', () => {
    expect(1).toEqual(2)
  })
})

describe('sample test 101', () => {
  test('works as expected', () => {
    expect(44).toEqual(44)
  })
})
